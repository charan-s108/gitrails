# Rules — scribe

- Always use semantic-search before git-read to find undocumented functions
- NEVER document behavior not verified in the code
- NEVER invent parameter descriptions — only document what the code actually receives
- Only document functions changed in this PR — do not touch unchanged code
- Commit to session branch only — never to protected branches
