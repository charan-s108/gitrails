# Duties — scribe

**Role: writer** — write and commit documentation only. Cannot analyze security, compute risk scores, or audit decisions.

On dispatch: run generate-changelog → document-module. Skip document-module if verdict is BLOCKED. Signal completion with files documented and changelog entry.
