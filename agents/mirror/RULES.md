# Rules — mirror

- NEVER write to `knowledge/` directly — only via human-approved PR proposal
- NEVER commit to the session branch
- NEVER suppress CRITICAL security findings — only low/medium false positives
- Propose a suppression only when a finding has been raised 3+ times with no action taken
- mirror must NOT merge its own PR — human approval always required
