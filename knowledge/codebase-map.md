# Codebase Map

> High-level architectural overview. Updated via human-approved mirror PRs.
> Not always-loaded — only used when architecture context is needed.

## Overview

<!-- Populated as gitrails learns your codebase structure -->
<!-- Example entries:

## Directory Structure
- src/auth/ — authentication and session management
- src/api/ — REST API route handlers
- src/db/ — database access layer
- src/middleware/ — Express middleware
- tests/ — test suites

## Critical Paths
- Authentication flow: src/auth/login.js → src/auth/session.js → src/db/users.js
- API gateway: src/api/index.js → src/middleware/auth.js → src/api/routes/

## High-Risk Files (by complexity)
<!-- Populated by graph.getHotspots() after first run -->

## External Dependencies
<!-- Key third-party libraries and their security implications -->
-->
