# gitrails — Python dependencies
#
# NOTE: gitrails runs primarily on Node.js 18+ via gitclaw.
# This file covers optional Python tooling used in hooks/scripts/
# and CI validation workflows.
#
# Install: pip install -r requirements.txt
# Recommended: use a virtual environment (python -m venv .venv)

# ── GitHub API interaction ─────────────────────────────────────────
PyGithub==2.3.0               # GitHub REST API client for hook scripts
requests==2.32.3              # HTTP client (PyGithub dependency)
urllib3==2.2.2                # HTTP library (requests dependency)

# ── YAML parsing (agent.yaml, hooks.yaml validation) ──────────────
PyYAML==6.0.2                 # Parse and validate gitagent YAML files
jsonschema==4.23.0            # JSON schema validation for tool definitions

# ── Security scanning support ─────────────────────────────────────
detect-secrets==1.5.0         # Entropy-based secret detection (supplements sentinel)
safety==3.2.9                 # CVE check against Python dependency manifests
bandit==1.8.3                 # Python static security analysis (OWASP patterns)

# ── Git operations ────────────────────────────────────────────────
GitPython==3.1.43             # Git repository access in hook scripts

# ── Webhook server (optional — for local development) ─────────────
fastapi==0.115.5              # Lightweight webhook receiver
uvicorn[standard]==0.32.1     # ASGI server for fastapi

# ── Observability ─────────────────────────────────────────────────
opentelemetry-sdk==1.28.0              # OTEL spans per agent run
opentelemetry-exporter-otlp==1.28.0   # Export to Grafana / Datadog / Honeycomb

# ── Testing (CI only) ─────────────────────────────────────────────
pytest==8.3.4                 # Test runner for hook script unit tests
pytest-asyncio==0.24.0        # Async test support
httpx==0.28.1                 # Async HTTP client for webhook tests

# ── Linting + formatting (dev only) ───────────────────────────────
ruff==0.8.6                   # Fast Python linter + formatter
